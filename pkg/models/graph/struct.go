package graph

import "expert_systems/pkg/models/edge"

type Graph []edge.Edge

// введите структуру графа как список попарных номеров вершин, являющихся началом и концом ребра
// например, NewGraph(1, 2, 2, 3, 3, 1)
func NewGraph(vertexNumbers ...int) Graph {
	if len(vertexNumbers) %

	edges := make([]edge.Edge, 0)
	for i := 0; i <= 8; i++ {

	}

	edges := make([]edge.Edge, 0)
	for i := 0; i <= 8; i++ {

	}

	return Graph(edges)
}
